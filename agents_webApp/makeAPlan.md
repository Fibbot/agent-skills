No private/undocumented APIs; avoid UIKit introspection hacks. Centralize OS-conditional behavior behind small wrappers (e.g., Platform, NavigationProvider), but don’t sprinkle #available everywhere. Use SwiftUI NavigationStack cleanly (no custom navigation routers for POC). Keep state management explicit (feature MVVM) and keep domain logic UI-agnostic. Avoid “clever” layout dependencies on current OS behavior; prefer standard components.Keep dependencies minimal; pin versions.